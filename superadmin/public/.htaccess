<IfModule mod_rewrite.c>
    RewriteEngine On

    RewriteBase /suretek-lat/superadmin/public/

    # Skip existing files/folders
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d

    RewriteRule ^(.*)$ index.php [L,QSA]
</IfModule>